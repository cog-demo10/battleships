name: CI

on:
  push:
  pull_request:

jobs:
  unit:
    name: Unit tests + coverage (engine/ai/game >= 90% lines)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm run coverage

